## Phase 20 — Query Result Cell Context Menu [DONE]
- Implemented the PRD interaction requirement: "Context menus should be available for schema nodes, table rows, grid cells, tabs, and saved queries."
- src/features/query-editor/QueryEditorTab.tsx:
  - Added a portal-based context menu for query result grid cells.
  - Right-clicking a result cell now selects it and opens actions for Copy Value, Copy Column Name, Copy Row as JSON, and Copy Row as CSV.
  - The menu closes on outside mouse down or Escape and uses the existing tokenized popover styling.
- Previously, query result cells only supported click selection and Cmd/Ctrl+C for a single value; row and column copy actions existed in the table viewer but not in query results.
- Checks passed: npm run type-check, npm run lint.

## Phase 19 — Table Viewer Toolbar [DONE]
- Implemented the PRD requirement: "Toolbars should be contextual but stable. Primary actions stay in the same area across tabs."
- src/features/table-viewer/TableViewerTab.tsx:
  - Added a toolbar (h-9, matching the query editor toolbar height) as the top-most element of the table viewer.
  - Left section: Table2 icon (schema-table color) + schema-qualified name (`schema.table`) with database name shown as a muted secondary label separated by a border.
  - Right section: connection badge (green dot + connection display name, same pattern as the query editor) + Refresh button.
  - Refresh button calls `refetch()` from React Query; the RefreshCw icon spins while a background fetch is in progress. Button is disabled while loading.
  - Added `connectionLabel` derived via `useMemo` from `activeSessions` + `profiles` — reads `profiles` and `activeSessions` from the store.
  - Also fixed two pre-existing TypeScript errors: `title` prop on Lucide SVG icons is not a valid SVG attribute; replaced with `aria-label` on the KeyRound and Link icons in the column header cell.
- Previously, the table viewer had no toolbar — it jumped directly to the filter bar with no context about what object was open or a way to refresh data outside of the error state.
- Checks passed: npm run type-check, npm run lint.

## Phase 18 — Recent Objects Sidebar Section [DONE]
- Implemented the PRD sidebar requirement: "Include connection groups, schema objects, saved queries, and recent objects."
- src/store/index.ts:
  - Added `RecentObject` interface (type, title, schema, table, database, connectionId, sessionId, savedQueryId, sql, timestamp).
  - Added `recentObjects` persisted state array (max 12 items, deduplicated by type+title+connectionId).
  - Added `addRecentObject` action that prepends new entries and trims to limit.
  - Added `clearRecentObjects` action.
  - Extended Zustand `partialize` to persist `recentObjects`.
- src/features/sidebar/RecentObjectsSection.tsx (new):
  - Renders a list of recently opened objects with type-specific icons (Table2 for tables, Eye for views, FileText for queries).
  - Each row is clickable — re-opens the table/view/query in a new tab with the original context.
  - Shows relative age on hover (e.g. "3m ago", "2h ago") using a `formatAge` helper.
  - Empty state with hint text when no recent objects exist.
  - "Clear recents" action appears when > 3 items for easy cleanup.
  - Styled with design token system: bg-hover/bg-pressed, motion-fast, radius-control, schema-table/schema-view/accent colors.
- src/features/schema/SchemaTree.tsx:
  - Calls `addRecentObject` when opening a table/view via context menu (open-table, open-query) or double-click.
- src/features/query-editor/SavedQueriesSection.tsx:
  - Calls `addRecentObject` when opening a saved query.
- src/components/CommandPalette.tsx:
  - Calls `addRecentObject` when opening a table or saved query from the palette.
- src/components/Sidebar.tsx:
  - Added "Recent" SidebarSection rendering RecentObjectsSection between Saved Queries and the resize handle.
- Previously, there was no way to quickly re-access recently opened objects without searching or browsing the schema tree again.
- Checks passed: npm run type-check, npm run lint.

## Phase 17 — Table Viewer Error State With Retry [DONE]
- Implemented the PRD interaction requirement: "Errors should explain what failed and offer the next action when possible."
- src/features/table-viewer/TableViewerTab.tsx:
  - Replaced the plain error text with a structured error panel: AlertCircle icon in a tinted container, "Failed to load table data" heading, descriptive error message, and a Retry button.
  - Retry button calls `refetch()` from the useQuery hook to re-attempt the table data fetch.
  - Error message extracts `.message` from Error instances for cleaner display.
  - Styling uses design token system: `bg-query-failed/10`, `text-query-failed`, `--radius-control`, `--shadow-hairline`, `--motion-fast`.
  - Added AlertCircle and RotateCw icon imports from lucide-react.
- Previously, the table viewer error state showed only a raw error string centered in the grid area with no way to retry — the user had to switch tabs or manually reload.
- Checks passed: npm run type-check, npm run lint.

## Phase 16 — Query Editor Type-Aware Cell Rendering [DONE]
- Upgraded the query editor result grid's CellValue to match the table viewer's type-aware rendering.
- src/features/query-editor/QueryEditorTab.tsx:
  - CellValue now accepts a `family: TypeFamily` prop and renders distinct treatments for NULL (pill with border), empty string ('') pill, boolean badges (green for true, muted for false), JSON monospace preview (truncated, data-json color), date/time tabular-nums, numeric right-aligned tabular-nums, and long text with truncation + title tooltip.
  - ResultGrid computes `families` array via `useMemo` from column `dataType` using the shared `getTypeFamily` helper.
  - Each cell passes its column's type family to CellValue.
  - Imported `getTypeFamily` and `TypeFamily` from the table-viewer types module to share the type classification logic.
- Previously, query editor results rendered all non-null values as plain monospace text — now they match the table viewer's visual treatment.
- Checks passed: npm run type-check, npm run lint.

## Phase 15 — Pre-fill SQL From Schema Tree Context Menu [DONE]
- Implemented the PRD requirement that "Open in query editor" from the schema tree context menu pre-fills the SQL editor with a useful query.
- src/features/schema/SchemaTree.tsx:
  - The "open-query" action now builds a quoted qualified name (`"schema"."table"`) and passes it as `queryContext.sql` with `SELECT * FROM "schema"."table" LIMIT 100;`.
  - Previously, the action opened an empty query tab with only `tableContext` set — the editor started blank.
  - The QueryEditorTab already reads `tab.queryContext?.sql` on mount, so the pre-filled SQL appears immediately.
- Checks passed: npm run type-check, npm run lint.

## Phase 14 — Wire Grid Row Density Setting [DONE]
- Connected the Data Grid row density setting (compact/comfortable/spacious) to the table viewer and query editor result grids.
- src/features/table-viewer/TableViewerTab.tsx:
  - Replaced hardcoded `ROW_HEIGHT = 33` with a `ROW_HEIGHT_BY_DENSITY` map (compact: 33, comfortable: 44, spacious: 56).
  - Reads `gridRowDensity` from the store and derives `rowHeight` dynamically.
  - Virtualizer `estimateSize`, row container height, and cell height all use the dynamic value.
  - Skeleton grid retains the compact height as a sensible loading default.
- src/features/query-editor/QueryEditorTab.tsx:
  - Replaced hardcoded `ROW_HEIGHT = 33` with the same `ROW_HEIGHT_BY_DENSITY` map.
  - `ResultGrid` reads `gridRowDensity` from the store directly and applies it to the virtualizer, row containers, and cells.
- Checks passed: npm run type-check, npm run lint.

## Phase 13 — Status Bar Enhancement [DONE]
- Implemented the PRD status bar requirement: "Left: active connection and database. Center: background work, if any. Right: last query duration, row count, or license state."
- src/store/index.ts:
  - Added `LastAction` interface (label, durationMs, rowCount, timestamp).
  - Added `lastAction` state and `setLastAction` action to the store.
- src/features/query-editor/QueryEditorTab.tsx:
  - After query execution, reports total duration and row count to `setLastAction`.
- src/features/table-viewer/TableViewerTab.tsx:
  - After table data loads, reports execution time and total row count to `setLastAction`.
- src/components/AppShell.tsx:
  - StatusBar now has three sections: left (connection + database), center (background task count from loading tabs), right (last action duration + row count).
  - Center section shows "N tasks running" with a spinner when any tab has `isLoading` set.
  - Right section shows row count and execution time from the last query or table load.
  - Added Loader2 icon import for the spinner.
- Checks passed: npm run type-check, npm run lint.

## Phase 12 — Table Viewer Column Header Key/Nullable Indicators [DONE]
- Implemented the PRD table viewer requirement: "Headers show column name, type badge, sort state, filter state, and nullable/key indicators."
- src-tauri/src/commands/data.rs:
  - Extended `ResultColumn` struct with `is_nullable`, `is_primary_key`, `is_foreign_key` fields.
  - `query_table` now fetches `is_nullable` from `information_schema.columns`.
  - Added PK lookup via `pg_index` + `pg_attribute` (collected into a HashSet).
  - Added FK lookup via `pg_constraint` + `pg_attribute` for foreign key columns.
  - `execute_sql` path defaults the new fields to false (no column metadata available from simple_query).
- src/features/table-viewer/types.ts:
  - Added `isNullable`, `isPrimaryKey`, `isForeignKey` to `ResultColumn`.
- src/features/query-editor/types.ts:
  - Added matching fields to the query editor's `ResultColumn` for type consistency.
- src/features/table-viewer/TableViewerTab.tsx:
  - `ColumnHeaderCell` now shows a KeyRound icon (schema-key color) for primary key columns.
  - Shows a Link icon (schema-foreign-key color) for foreign key columns.
  - Shows a `?` indicator (tertiary color) for nullable columns.
- src/styles/tokens.css:
  - Added `--schema-foreign-key` token mapped to `--ds-syntax-keyword` (purple).
- src/styles/index.css:
  - Added `--color-schema-foreign-key` Tailwind mapping.
- Checks passed: npm run type-check, npm run lint, cargo clippy --workspace --all-targets -- -D warnings.

## Phase 11 — Connection Empty-State Paste URL [DONE]
- Implemented the next incomplete PRD sidebar empty-state gap: users with no saved connections now get a Paste URL shortcut next to New Connection.
- src/features/connections/ConnectionList.tsx:
  - Added a secondary Paste URL action to the no-connections empty state with a clipboard icon and hover/pressed states.
- src/components/Sidebar.tsx:
  - Reads the clipboard on Paste URL, opens the new connection sheet, and passes the clipboard contents as the initial connection URL.
- src/features/connections/ConnectionForm.tsx:
  - Accepts an initial URL, preloads it into the quick-connect field, and applies parsed host, port, database, username, password, and a default display name.
  - Resets sheet state on open so create/edit/paste flows do not leak previous form values.
- src/features/connections/api.ts:
  - Extended PostgreSQL URL parsing to include the password.
- Checks passed: npm run type-check, npm run lint, git diff --check.

## Phase 10 — Schema Load Error Rows [DONE]
- Implemented the next incomplete PRD sidebar state gap: schema tree lazy-load failures now render inline where the loading row would appear.
- src/features/schema/SchemaTree.tsx:
  - Added compact error rows with an Alert icon and semantic query-failed color.
  - Database, schema, object, and column query failures now create inline error rows.
  - Each error row includes a focused/hoverable Retry action wired to the owning React Query `refetch`.
  - Added safe fallback text for non-Error thrown values.
- src/features/schema/types.ts:
  - Extended schema error nodes with an optional `onRetry` callback.
- src/features/table-viewer/TableViewerTab.tsx:
  - Removed unused density locals that were causing the repo lint gate to fail.
- Checks passed: npm run type-check, npm run lint, git diff --check.

## Phase 09 — Complete Token System [DONE]
- Found that the PRD required three token groups missing from tokens.css: semantic state, data value accents, and query accents.
- src/styles/tokens.css:
  - Added `/* Semantic state */` group: --state-success/warning/danger/info/pending/disconnected/loading, all backed by existing --ds-* palette values.
  - Added `/* Data value accents */` group: --data-null/empty/boolean-true/boolean-false/json/date/number/text/modified/filtered/sorted.
  - Added `/* Query accents */` group: --query-running/succeeded/failed/duration/row-count/transaction/diagnostic.
- src/styles/index.css:
  - Added Tailwind @theme inline mappings for schema accent colors (--color-schema-*), query state colors (--color-query-*), data value colors (--color-data-*), and --color-inverse (text on colored fills).
- src/features/schema/SchemaTree.tsx:
  - Updated schema object icon classes from ad-hoc colors to semantic schema accent tokens: database=text-schema-database (accent blue), schema=text-schema-schema (purple), table=text-schema-table (green), view=text-schema-view (blue), sequence=text-schema-sequence (orange), function=text-schema-function (amber).
- src/features/query-editor/QueryEditorTab.tsx:
  - RunButton success state: bg-green-600 → bg-query-succeeded; error state: bg-red-600 → bg-query-failed; text-white → text-inverse.
  - Connection status dot: bg-green-500 → bg-query-succeeded.
  - ErrorSummary panel: bg-red-500/8, border-red-500/20, text-red-500, text-red-400/70 → bg-query-failed/*, text-query-failed/*.
  - ResultSection inline error: text-red-500 → text-query-failed.
- src/features/table-viewer/TableViewerTab.tsx:
  - JSON cell preview: text-secondary → text-data-json (blue) so JSON values are visually distinct from plain text.
- Checks passed: npm run type-check, npm run lint.

## Phase 08 — App Icon [DONE]
- Implemented the app icon per the PRD App Icon section.
- tools/make_icon.py:
  - Python script that generates all required Tauri icon assets from scratch.
  - Background: macOS squircle (corner radius ~22.37% of size) filled with Tairiki blue `#0070c1`.
  - Glyph: Phosphor Icons `binoculars-bold.svg` path, scaled to ~60% of canvas width, white fill, centered.
  - Subtle inner stroke at 8% white opacity for depth.
  - Uses `cairosvg` for SVG→PNG conversion and `iconutil` for .icns bundle.
  - ICO file written manually with embedded PNG data at 16, 32, 48, 256 px.
- src-tauri/icons/:
  - Replaced placeholder PNGs with crafted icon: `32x32.png`, `128x128.png`, `128x128@2x.png`.
  - Added new files: `icon.png` (512×512), `icon.icns` (macOS multi-res bundle), `icon.ico` (Windows 4-size).
- src-tauri/tauri.conf.json:
  - Extended `bundle.icon` to include `icons/icon.png`, `icons/icon.icns`, `icons/icon.ico`.
- Checks passed: npm run type-check, npm run lint, cargo clippy --workspace --all-targets -- -D warnings.

## Phase 07.14 — Verification [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.14 as the final UI craft verification task.
- Ran all automated checks across the full codebase:
  - `npm run type-check` (tsc --noEmit): passed with no errors.
  - `npm run lint` (eslint): passed with no errors or warnings.
  - `cargo clippy --workspace --all-targets -- -D warnings`: passed with no warnings.
- All phases 7.1 through 7.13 confirmed complete per progress.txt.
- No code changes required; verification pass completed cleanly.

## Phase 07.13 — Add Menus And Micro-Interactions [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.13 as the next incomplete UI craft task.
- src/store/index.ts:
  - Added `closeOtherTabs(id)` — closes all tabs except the given one.
  - Added `closeTabsToRight(id)` — closes all tabs after the given one; active tab updated if it was among the closed ones.
- src/components/TabBar.tsx:
  - `TabContextMenu` component: renders via `createPortal` on right-click; items are Close Tab, Close Others (disabled when no others), Close to Right (disabled if last tab), and Copy Title. Closes on Escape or mousedown outside.
  - `TabItem` now accepts `onContextMenu` prop; hover transition uses `duration-[var(--motion-fast)]`.
  - `TabBar` manages context menu state and renders `TabContextMenu`.
- src/features/connections/ConnectionList.tsx:
  - `ConnectionContextMenu` gains `onDuplicate` prop; renders a "Duplicate" item (Copy icon) between Edit and the destructive separator.
  - `ConnectionRow` gains `onDuplicate` prop; wired to `handleDuplicate` in the parent.
  - `handleDuplicate` calls `connectionsApi.create` with the same profile fields, display name suffixed with "(copy)", and empty password; refreshes the list on success.
- src/features/query-editor/SavedQueriesSection.tsx:
  - `QueryRow` now receives the full `SavedQuery` object plus `onRename` and `onDuplicate` callbacks.
  - Inline rename: "Rename" in dropdown sets `isRenaming` state; text span becomes a styled input pre-filled with the current name; Enter or blur commits, Escape cancels.
  - "Duplicate" calls `savedQueriesApi.save` with name suffix "(copy)" and the same sql/folder; result invalidates the saved-queries cache.
  - Dropdown now shows Open, Rename, Duplicate, (separator), Delete.
  - Hover/disclosure transitions use `duration-[var(--motion-fast)]` and `duration-[var(--motion-base)]` tokens.
- src/styles/index.css:
  - Added `@media (prefers-reduced-motion: reduce)` block: collapses all animation durations to `0.01ms` and iteration counts to 1, respecting OS-level motion preference.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.12 — Polish Query Editor [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.12 as the next incomplete UI craft task.
- src/features/query-editor/QueryEditorTab.tsx:
  - `RunButton` component: state machine idle → pending → success → idle / error → idle with 1.5s auto-reset; distinct icons (Play/Loader/Check/X) and colors (accent/green/red).
  - Keyboard shortcut hints rendered inline on Run (⌘↵) and Save (⌘S) buttons in the toolbar.
  - Save button shows accent color + small dot when `isDirty` (sql differs from last saved baseline).
  - Result panel height persisted to localStorage (`esploro-query-result-height`); restored on mount.
  - `EmptyResultState` component: shown in the result pane before first run (terminal icon + shortcut hint); pane is revealed after the first run attempt so the empty state replaces blank editor space.
  - `ErrorSummary` panel: appears above result pane with error message + SQLSTATE code when a query errors.
  - `ResultSection` refined: compact header, tabular-nums ms, "No rows returned" empty state for queries returning columns with 0 rows.
  - Connection badge truncates long names with `max-w-[180px]`; status dot dims when disconnected.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.11 — Polish Table Viewer [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.11 as the next incomplete UI craft task.
- src/features/table-viewer/TableViewerTab.tsx:
  - `CellValue` is now type-aware: NULL pill, empty-string `''` pill, boolean badge (green for true, muted for false), JSON monospace truncated preview, date/time tabular-nums, numeric tabular-nums right-aligned, long text truncated with title tooltip.
  - `SkeletonGrid` component: animated skeleton header + 8 staggered rows shown during initial load instead of a plain spinner; uses fixed widths with per-cell animation delay for a realistic stagger.
  - `ColumnHeaderCell` now shows a Filter icon and faint accent tint when a filter is active on that column.
  - `CellContextMenu` replaces the old `RowContextMenu`: right-clicking any cell shows Copy Value, Copy Column Name, separator, Copy Row as JSON, Copy Row as CSV; clicking also selects the cell before the menu opens.
  - Distinct empty states: empty table (Database icon + "This table is empty" + subtext) vs filtered-empty (Filter icon + active filter count + "Clear filters" button).
  - Footer shows execution time only when > 0ms; row counts formatted with `toLocaleString`.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.10 — Polish Command Palette [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.10 as the next incomplete UI craft task.
- src/components/CommandPalette.tsx:
  - Replaced loose `Command` interface with typed `CommandResult` (title, subtitle, shortcut, action, group).
  - Added `GroupHeader` component rendering divider-separated group labels (Connections, Schema, Queries, Commands, Settings).
  - Connection commands now show `user@host:port/db` as subtitle; table commands show profile name and host.
  - "New Query" command shows ⌘T shortcut hint; added "Appearance Settings" command.
  - Curated default view when query is empty: core commands + connections + saved queries (no table flood).
  - `NoResults` component with explanatory text and "New Query" / "New Connection" action buttons.
  - Schema loading spinner in the search bar when object queries are in-flight.
  - Footer keyboard hint bar (↑↓ navigate, ↵ select, ESC close).
  - Styling updated to use `--surface-*`, `--border-*`, `--text-*`, `--motion-fast`, `--radius-control` tokens from Phase 7.2.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.9 — Polish Sidebar [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.9 as the next incomplete UI craft task.
- src/features/connections/ConnectionList.tsx:
  - Extracted `ConnectionRow` sub-component for cleaner structure.
  - Added proper empty state: icon, heading, subtext, and "New Connection" CTA button (wired via new `onNewConnection` prop).
  - Added secondary metadata line per connection row: `user@host:port/db`.
  - Added `Loader2` spinner replacing status dot while connecting.
  - Added right-click context menu (`ConnectionContextMenu`) via `createPortal`: connect/disconnect, edit, delete with separator.
  - Hover/selected states use new `bg-hover` / `bg-selected` / `bg-pressed` tokens.
- src/components/Sidebar.tsx:
  - Passes `onNewConnection={openCreate}` to `ConnectionList`.
- src/features/schema/SchemaTree.tsx:
  - Replaced "Loading…" text rows with animated skeleton bars (3 staggered widths, `animate-pulse`).
  - Improved error row with consistent icon-area spacing.
- src/features/query-editor/SavedQueriesSection.tsx:
  - Empty state now shows two-line hint ("Run a query and press Save (⌘S)").
  - `FolderGroup` rows now show a chevron with disclosure animation.
  - `QueryRow` hover and menu-trigger styles use `bg-hover` / `bg-pressed` tokens.
  - Dropdown content uses `bg-raised` token.
- src/styles/index.css:
  - Added `--color-hover`, `--color-pressed`, `--color-raised`, `--color-selected` to `@theme inline` mapped from surface tokens.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.8 — Polish App Shell [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.8 as the next incomplete UI craft task.
- src/store/index.ts:
  - Added `isDirty` and `isLoading` optional fields to the Tab interface.
  - Added `setTabDirty(id, isDirty)` and `setTabLoading(id, isLoading)` actions.
- src/components/TabBar.tsx:
  - Added type icon per tab: Database for table tabs, Terminal for query tabs, Settings for settings tabs.
  - Loading spinner replaces icon when `isLoading` is true.
  - Small accent dot appears when `isDirty` is true (and not loading).
  - Added `group` class so close button reveals on hover for all tabs (was only visible when active).
- src/components/AppShell.tsx:
  - Added `StatusBar` component at the bottom of the right panel: shows active connection name and database from the active tab context.
  - Improved `WelcomeView`: Database icon, clearer heading based on connection state, two-line keyboard hint.
- src/components/Sidebar.tsx:
  - Widened resize handle from 4px to 6px; added hover/active accent color for discoverability.
- src/features/query-editor/QueryEditorTab.tsx:
  - Wires `setTabDirty` via `useEffect` comparing current SQL to baseline at mount; clears on unmount and after a successful save.
- src/features/table-viewer/TableViewerTab.tsx:
  - Wires `setTabLoading` from `isLoading || isFetching`; clears on unmount.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.7 — Update CodeMirror Theme [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.7 as the next incomplete UI craft task.
- src/features/query-editor/tairikiTheme.ts:
  - Moved font variables (--font-editor, --font-editor-size, --font-editor-line-height) into the base `&` selector so the theme is self-contained.
  - Replaced all --ds-* raw palette references with phase-7.2 craft tokens: --surface-base/sidebar/hover, --border-subtle/default, --text-primary/tertiary/inverse, --editor-syntax-* for highlight style.
  - Added autocomplete popup polish: per-item font/size, detail color, consistent padding, shadow-popover, radius-popover.
  - Added lint/diagnostic styling: wavy underlines for error/warning ranges, colored left-border panels.
- src/features/query-editor/SqlEditor.tsx:
  - Removed duplicate inline EditorView.theme font override (now handled by tairikiTheme).
- Checks passed: npm run type-check, npm run lint.

## Phase 07.6 — Build Appearance Settings [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.6 as the next incomplete UI craft task.
- src/features/settings/AppearanceSettings.tsx:
  - Replaced the simple theme picker with a crafted live preferences panel for theme, UI font, editor font, UI/editor font sizes, and editor line height.
  - Added common font quick-picks, styled number/range controls, invalid input states, save/error status text, and reset-to-defaults confirmation when customized.
  - Applies valid changes immediately through the shared preference helpers, persists them with `set_ui_preferences`, updates the bootstrap cache, and hydrates the store theme.
  - Added a preview surface showing sidebar, SQL editor, table cell, badge, and status examples using the selected UI/editor font values.
- Checks passed: npm run type-check, npm run lint, git diff --check.

## Phase 07.5 — Apply Preferences Before First Paint [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.5 as the next incomplete UI craft task.
- index.html:
  - Replaced the theme-only bootstrap with a pre-React preference bootstrap.
  - Reads `esploro-ui-preferences` from localStorage, normalizes theme/font values, and applies `data-theme` plus UI/editor CSS font variables before the app bundle loads.
  - Preserves legacy Zustand theme fallback when the new bootstrap cache is absent.
- src/features/settings/preferences.ts:
  - Added shared helpers to apply normalized UI preferences to the document and cache a bootstrap snapshot.
- src/components/AppShell.tsx:
  - Loads canonical preferences from Tauri after React starts, reapplies them, updates the bootstrap cache, and hydrates the store theme without writing preferences back.
- src/store/index.ts:
  - Updates the bootstrap cache and document variables when theme changes are persisted.
- Checks passed: npm run type-check, npm run lint, git diff --check.

## Phase 07.4 — Persist Preferences In Tauri [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.4 as the next incomplete UI craft task.
- src-tauri/src/commands/license.rs:
  - Added typed `UiPreferences`/UI/editor preference structs with backend validation for theme, font family, font sizes, and editor line height.
  - Added `get_ui_preferences` and `set_ui_preferences` Tauri commands.
  - Preserved the legacy `uiTheme` key while writing the new nested `ui` and `editor` prefs.
  - Switched prefs writes to an atomic temp-file then rename flow.
  - Made malformed `prefs.json` fall back to defaults with a warning instead of blocking boot.
- src-tauri/src/lib.rs:
  - Registered the typed UI preference commands and removed the old loose `set_ui_pref` command registration.
- src/store/index.ts:
  - Updated theme persistence to merge with canonical backend preferences and write through `set_ui_preferences`.
- Checks passed: npm run type-check, npm run lint, cargo clippy --workspace --all-targets -- -D warnings.

## Phase 07.3 — UI Preference Model [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.3 as the next incomplete UI craft task.
- src/features/settings/preferences.ts:
  - Added the shared `UiPreferences` model, canonical theme values, defaults, safe ranges, and normalization helpers for bad stored values.
  - Added legacy `light`/`dark` theme normalization so existing localStorage snapshots still resolve correctly.
- src/store/index.ts, src/features/settings/AppearanceSettings.tsx, src/components/CommandPalette.tsx:
  - Switched theme typing and defaults to the shared preferences model.
  - Updated theme actions to use canonical Tairiki theme values.
- src/components/AppShell.tsx and index.html:
  - Mapped canonical preferences back to the current `data-theme="light"|"dark"` contract at runtime and before React hydration.
- Checks passed: npm run type-check, npm run lint.

## Phase 07.2 — Expanded Design Tokens [DONE]
- Followed `plans/07-ui-craft.md` Phase 7.2 after the Phase 7.1 audit identified token expansion as the next task.
- src/styles/tokens.css:
  - Added craft-level token contract for surfaces, borders, text, schema accents, editor syntax, motion, radius, shadows, and fonts.
  - Kept the new tokens mapped to the existing Tairiki `--ds-*` values so light, dark, and system-driven theme changes continue to flow through one palette.
- src/styles/index.css:
  - Mapped Tailwind `font-mono` to the new `--font-editor` runtime variable.
  - Switched body font size, color, background, and focus outline to the new runtime token aliases.
- src/features/query-editor/SqlEditor.tsx:
  - Updated CodeMirror font family, font size, and line height to read from the new editor font tokens.
- Checks passed: npm run type-check, npm run lint, git diff --check.

## Phase 07.1 — UI State Audit [DONE]
- Read the active PRD and Phase 07 plan, then inspected the listed UI surfaces before making component changes.
- Added `plans/07-ui-craft-audit.md` with current state coverage for:
  - App shell, sidebar, sidebar sections, tabs, command palette
  - Connection list, schema tree, table viewer, query editor, SQL editor, appearance settings
  - Token/global CSS coverage
- Identified the next task as Phase 7.2: expand the design token contract before deeper component polish.
- Documentation-only change; no test run required.

## Phase 4 — Appearance Settings UI [DONE]
- PRD Phase 4 of the Tairiki theme implementation roadmap
- src-tauri/src/commands/license.rs:
  - Added ui_theme: Option<String> field to UserPrefs (serde default = None)
  - Added set_ui_pref(key, value) Tauri command: writes ui.theme to prefs.json
- src-tauri/src/lib.rs: registered set_ui_pref command
- index.html: inline anti-FOUC <script> reads esploro-ui localStorage synchronously
  before React hydrates; sets data-theme="light"/"dark" to prevent flash of unstyled content
- src/features/settings/AppearanceSettings.tsx (new):
  - Segmented control with three options: Tairiki Light | Tairiki Dark | System
  - Active option: bg-content shadow-sm text-label with accent-tinted icon
  - Calls useAppStore setTheme on selection (which persists to both localStorage and prefs.json)
- src/features/settings/index.ts (new): barrel export
- src/components/AppShell.tsx:
  - Settings tab now renders AppearanceSettings + divider + LicenseSettings in a shared p-8 max-w-lg container
- src/features/license/LicenseSettings.tsx:
  - Converted from standalone page (div with p-8 max-w-lg) to a <section> with sidebar-style h3 header
- src/store/index.ts:
  - setTheme now fire-and-forgets invoke("set_ui_pref", { key: "ui.theme", value }) to persist to prefs.json
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Phase 3 — CodeMirror Tairiki Theme [DONE]
- PRD Phase 3 of the Tairiki theme implementation roadmap
- src/styles/tokens.css:
  - Added --ds-syntax-operator (light_purple: #693988 light / #c397d8 dark) to all three blocks
  - Added --ds-syntax-comment (#cd0009 light / #a89984 dark) to all three blocks
- src/features/query-editor/tairikiTheme.ts (new):
  - Static CodeMirror Extension; no JS-side theme switching required
  - EditorView.theme: editor chrome (bg, gutters, cursor, selection, active line,
    bracket matching, search match, autocomplete tooltip) via CSS vars
  - HighlightStyle: keywords/modifiers → syntax-keyword (purple), functions →
    syntax-number (blue), strings → syntax-string (green), numbers → syntax-number,
    types/namespaces → syntax-type (amber), constants → syntax-enum (orange),
    operators/punctuation → syntax-operator (light_purple), comments → syntax-comment
    (italic), booleans/null/regexp → syntax-special (red)
  - Theme follows CSS custom properties automatically; no rebuild on theme switch
- src/features/query-editor/SqlEditor.tsx:
  - Removed oneDark import and isDark prop
  - Replaced with tairikiTheme; removed .cm-gutters override (handled by tairikiTheme)
- All CI checks pass (tsc --noEmit, eslint)

## Phase 2 — Connection List & Sidebar Polish [DONE]
- PRD Phase 2 of the Tairiki theme implementation roadmap
- src/features/connections/ConnectionList.tsx:
  - Status dot: replaced hardcoded #34C759 fill with var(--ds-success) so it follows the active theme
  - Sidebar focused item: swapped accent/10 ring highlight → bg-active background + 2px border-accent left border (per spec)
  - Hover state: changed hover:bg-control → hover:bg-subtle on both connection rows and folder rows
  - Folder chevron: added duration-150 ease-out to the rotation transition
- src/components/TabBar.tsx:
  - Active tab: added border-b-2 border-b-accent bottom indicator
  - Inactive tabs: border-b-2 border-b-transparent to prevent layout shift on activation
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Keyboard Navigation — Connection List [DONE]
- PRD required "Keyboard-navigable sidebar (arrow keys, Enter to open)"; schema tree had it, connection list did not
- src/features/connections/ConnectionList.tsx:
  - NavItem union type (connection | folder); navKey() helper
  - focusedKey: string | null state; focusedIndex derived each render
  - containerRef + CSS.escape querySelector to scroll focused row into view
  - onKeyDown on outer div (tabIndex=0, outline-none):
    - ArrowDown/ArrowUp: move focus through flat navItems list (folder-aware, respects collapsed)
    - ArrowRight: expand collapsed folder; no-op otherwise
    - ArrowLeft: collapse expanded folder; no-op otherwise
    - Enter: connect/disconnect for connections, toggle collapse for folders
  - Focused item rendered with bg-accent/10 + ring-1 ring-inset ring-accent/30 highlight
  - Action buttons (connect/disconnect/edit/delete) reveal on focus as well as hover
  - onBlur clears focus when focus leaves the container entirely
  - Button onClick handlers now call e.stopPropagation() to avoid triggering container's click
- src/features/schema/SchemaTree.tsx:
  - Added e.stopPropagation() to all handled keys (ArrowDown/Up/Left/Right/Enter) so nested
    schema tree events don't bubble up and trigger the parent connection list handler
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Connection Folders — Sidebar Grouping [DONE]
- PRD required "Connections sidebar grouped by user-defined folders"; prior impl rendered all connections flat
- src/features/connections/ConnectionList.tsx:
  - Ungrouped connections (no folder set) rendered at top, unchanged
  - Connections with a folder= value grouped into collapsible folder sections below
  - Folders sorted alphabetically; each folder header shows ChevronRight + Folder icon + name
  - collapsedFolders: Set<string> state; all folders expanded by default; toggleFolder flips membership
  - Connections inside a folder rendered with pl-6 indent to visually nest them
  - renderConnection() extracted as an inner function; handles both flat and indented cases
- No Rust changes needed (folder field already persisted by Phase 02 connection backend)
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Schema Tree — Fuzzy Search [DONE]
- PRD required "fuzzy, case-insensitive" search on the schema tree; prior impl used substring only
- src/lib/fuzzy.ts: extracted fuzzyScore(str, query) → 0/1/2 as shared utility
- src/components/CommandPalette.tsx: now imports fuzzyScore from lib/fuzzy (local copy removed)
- src/features/schema/SchemaTree.tsx: search results scored and sorted descending; leaves collected
  with scores, sorted, then db/schema ancestor rows injected (deduped) above their matches
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Command Palette — Fuzzy Search + Keyboard Navigation [DONE]
- src/components/CommandPalette.tsx
  - fuzzyScore(str, query): returns 2 (substring match), 1 (fuzzy: all chars in order), 0 (no match)
  - Filtered results sorted by score descending (substring matches ranked above fuzzy)
  - selectedIdx state: resets to 0 on query/results change; itemRefs array for scroll-into-view
  - onKeyDown on the search input: ArrowDown/ArrowUp move selection (clamped), Enter selects
  - onMouseEnter on each item updates selectedIdx (mouse and keyboard stay in sync)
  - Selected item rendered with bg-accent/10 highlight; icon tinted text-accent
  - Scroll-into-view (block: nearest) fires on selectedIdx change via useEffect
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Keyboard Navigation — Schema Tree [DONE]
- src/features/schema/SchemaTree.tsx
  - focusedKey state tracks the focused row by its unique key (survives tree expansion)
  - focusedIndex derived from focusedKey via items.findIndex (re-derived on each render)
  - handleKeyDown on the outer container div (tabIndex=0, outline-none):
    - ArrowDown / ArrowUp: move focus to next/previous navigable item (skips loading/error)
    - ArrowRight: expand collapsed node; no-op if already expanded
    - ArrowLeft: collapse expanded node; if already collapsed, jump to nearest ancestor
      (walks backwards to find first item with lower nodeDepth)
    - Enter: open table/view in table viewer tab; toggle expand for other node types
  - Focused item rendered with bg-accent/10 + ring-1 ring-inset ring-accent/30 highlight
  - useEffect scrolls focused child into view (block: nearest) on focusedKey change
  - onBlur clears focus when focus leaves the container entirely
  - onClick on every TreeRow calls onFocus (setFocusedKey) + onToggle if expandable
  - isNavigable / nodeDepth module-level helpers (loading/error rows skipped)
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Phase 06 — Licensing [DONE]
- Rust: src-tauri/src/commands/license.rs — full offline licensing system
  - HMAC-SHA256 key verification using hmac + sha2 crates; signing key from
    ESPLORO_LICENSE_SECRET_KEY env var at compile time (dev fallback included)
  - LicensePayload: version, tier, issued_at, expires_at, licensee, max_seats, key_id
  - LicenseTier enum: Personal | Commercial | Unlicensed
  - LicenseStatus: tier, licensee, expiresAt, daysUntilExpiry, bannerVisible,
    gracePeriodEnds, showUsageDialog
  - UserPrefs: usage_type_answered, usage_type, first_launch, commercial_detected_at
    persisted to $APP_DATA_DIR/prefs.json
  - License key stored at $APP_DATA_DIR/license.key
  - 6 Tauri commands: get_license_status, activate_license, deactivate_license,
    answer_usage_dialog, dismiss_license_banner, notify_connection_count
  - open_license_url: macOS `open` command to ESPLORO_STRIPE_URL (env var, dev fallback)
  - Banner logic: shown when commercial_detected_at set + 14-day grace elapsed + not
    session-dismissed (banner_dismissed bool in AppState, not persisted)
  - Usage dialog: shown 3 days after first launch if usage not answered and no license
  - Heuristic: ≥4 connections sets commercial_detected_at
- Rust: lib.rs — added banner_dismissed: Mutex<bool> to AppState; registered 7 new commands
- Rust: Cargo.toml — added hmac 0.12, sha2 0.10, base64 0.22
- Rust: Cargo.toml (workspace) — added tools/keygen to workspace members
- tools/keygen/ — standalone CLI for generating signed license keys
  - Usage: ESPLORO_LICENSE_SECRET_KEY=... keygen --licensee "Acme" [--expires YYYY-MM-DD]
  - Uses clap for arg parsing; outputs ESPLORO-<payload_b64>.<sig_b64>
- Frontend: src/features/license/
  - types.ts: LicenseTier, LicenseStatus
  - api.ts: licenseApi + LICENSE_STATUS_KEY React Query key constant
  - LicenseBanner.tsx: amber banner at bottom of right panel; session-dismiss; links to
    activation sheet and Stripe URL; only renders when bannerVisible
  - LicenseActivationSheet.tsx: Radix Dialog slide-in from right; textarea for key input,
    ⌘↵ shortcut; inline error display; auto-closes on success
  - UsageTypeDialog.tsx: Radix Dialog modal; radio personal/commercial; shown when
    showUsageDialog is true in license status
  - LicenseSettings.tsx: settings tab content; shows tier/licensee/expiry for commercial;
    get/activate/remove actions for personal/unlicensed
  - index.ts: exports all components and api
- store/index.ts: added "settings" to TabType
- AppShell.tsx: renders LicenseBanner below main content, UsageTypeDialog globally;
  LicenseSettings for settings tabs; useEffect notifies Rust of profile count changes
- CommandPalette.tsx: "License Settings" command opens a settings tab (KeyRound icon)
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Phase 05 — SQL Query Editor [DONE]
- Rust: execute_sql Tauri command in src-tauri/src/commands/data.rs
  - Multi-statement execution: splits on ';', runs each via simple_query, stops on first error
  - QueryResult: columns, rows (all Option<String>), rows_affected (u64 from CommandComplete), execution_ms, error
  - QueryError: message, position (byte offset from Postgres ErrorPosition::Original), SQLSTATE code
  - simple_query returns everything as strings; no parameter binding needed for raw SQL
- Rust: new src-tauri/src/commands/saved_queries.rs
  - save_query (create or update by id), list_saved_queries, get_saved_query, delete_saved_query
  - Persisted to $APP_DATA_DIR/saved_queries.json; grouped by optional folder field
- Registered execute_sql + 4 saved_queries commands in lib.rs
- Frontend: src/features/query-editor/
  - types.ts: ResultColumn, QueryError, QueryResult, SavedQuery
  - api.ts: queryEditorApi.executeSql, savedQueriesApi (list/save/get/delete)
  - SqlEditor.tsx: CodeMirror 6 wrapper — SQL highlighting, line numbers, fold gutter, bracket
    matching, autocompletion, ⌘↵ keymap; uses refs to avoid stale closures; reconfigures
    extensions on schema/dark-mode changes; syncs external value changes; setDiagnostics for errors
  - QueryEditorTab.tsx: full tab component
    - Toolbar: connection badge (green dot, active session name), Run/Save buttons with spinner
    - Schema-aware completions built from React Query object cache (same cache as CommandPalette)
    - useMutation for executeSql; result state held locally
    - ResultPanel: stacked ResultSection per statement (header with row count / rows affected / ms)
    - ResultGrid: virtualized body (TanStack Virtual, overscan 10, 33px rows), sticky header
      scroll-synced via JS, cell click-to-select, ⌘C copies raw value
    - Resizable result panel (drag handle, min 80px)
    - SaveDialog: name + folder inputs, ⌘S shortcut, invalidates saved-queries React Query cache
  - SavedQueriesSection.tsx: groups queries by folder, click opens in new tab, right-click → delete
  - index.ts: exports QueryEditorTab, SavedQueriesSection, types
- store/index.ts: added queryContext?: { sql, savedQueryId } to Tab interface
- AppShell.tsx: renders QueryEditorTab; ⌘T opens new query tab, ⌘W closes active tab
- Sidebar.tsx: SavedQueriesSection wired into Saved Queries section
- CommandPalette.tsx: 'New Query' command; saved queries from cache as 'Open: {name}' commands
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Phase 04 — Table Viewer [DONE]
- Rust: query_table Tauri command in src-tauri/src/commands/data.rs
  - TableQueryRequest with filters, sort, pagination; all identifiers validated (^[a-zA-Z_][a-zA-Z0-9_$]*$)
  - FilterOperator (Eq/Neq/Like/ILike/Gt/Lt/Gte/Lte/IsNull/IsNotNull), SortDirection (Asc/Desc)
  - SQL built dynamically: columns fetched from information_schema, cast to ::text for uniform deserialisation
  - Filter values parameterised ($1, $2, …); column/table/schema identifiers validated then double-quoted
  - Type-aware filter casts: numeric → bigint/numeric, date → timestamptz, etc.
  - Data and COUNT queries run concurrently via tokio::join! on two pool connections
  - Returns TableQueryResult: columns, rows (all Option<String>), total_count, page, page_size, execution_ms
- Registered query_table in lib.rs invoke_handler
- Frontend: src/features/table-viewer/
  - types.ts: FilterOperator, SortDirection, ColumnFilter, TableQueryRequest, TableQueryResult, ResultColumn
    plus getTypeFamily, getOperatorsForFamily, OP_LABELS helpers
  - api.ts: tableApi.queryTable wrapping invoke
  - TableViewerTab.tsx: full table viewer component
    - Filter bar: per-column compact inputs (operator select + value input), horizontally scrollable
    - Filter debouncing: 300ms before triggering re-fetch; resets to page 0 on change
    - Active filter chips with × remove and "Clear all" button
    - Grid: separate sticky header (JS scroll-sync) + TanStack Virtual body (overscan 10, 33px rows)
    - Column headers: name + data-type badge, click to sort (asc → desc → none)
    - NULL values rendered as italic grey NULL pill
    - Focused cell: click to select, ⌘C copies raw value
    - Right-click row → context menu: Copy row as JSON / Copy row as CSV
    - Pagination footer: "Showing X–Y of N rows (Zms)" + Prev/Next buttons
    - keepPreviousData via React Query v5 placeholderData
- AppShell.tsx: renders TableViewerTab for table-type tabs (overflow-hidden for full-bleed layout)
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Phase 01 — Project Scaffold [DONE]
- Tauri 2 + React 19 + TypeScript + Tailwind CSS 4 project initialized
- macOS window config: title bar overlay, vibrancy sidebar effect
- Design token CSS variables (light + dark mode)
- AppShell with resizable sidebar, TabBar, WelcomeView
- SidebarSection collapsible component
- CommandPalette (⌘K) stub with Radix UI Dialog
- Zustand store: tabs, sidebar width (persisted), command palette state
- CI: cargo clippy, tsc --noEmit, eslint (all green)
- Placeholder RGBA icons for build

## Phase 03 — Schema Browser [DONE]
- Rust: 4 new Tauri commands — list_databases, list_schemas, list_objects, list_columns
- list_objects returns tables (with estimated row count), views, sequences, functions for a schema
- list_columns returns full column metadata: name, type, nullable, default, PK/FK flags
- Frontend: TypeScript types (TreeNode union, ColumnDef, SchemaObjects, etc.)
- Frontend: API layer wrapping 4 invoke calls
- Frontend: SchemaTree component — lazy-loading tree using useQuery + useQueries per level
  - Databases → schemas → groups (Tables/Views/Sequences/Functions) → items → columns
  - Search box filters across loaded tables/views in real time
  - Right-click context menu: copy name, copy qualified name, open table viewer, open in query editor
  - Double-click table/view opens a table tab
  - Expanded node state persisted per connection in localStorage
- ConnectionList: added renderExpansion prop; SchemaTree rendered inline below each active connection
- Sidebar: passes SchemaTree as renderExpansion
- CommandPalette: table search across React Query cache (loaded schemas only)
- store/index.ts: added expandedNodes + toggleNode (persisted); Tab extended with tableContext
- All CI checks pass (cargo clippy, tsc --noEmit, eslint)

## Phase 02 — Connection Management [DONE]
- Rust: ConnectionProfile + SslMode data model (stored in connections.json, no password in JSON)
- Rust: AppState with sessions HashMap (session_id → pool + connection_id)
- Rust: 7 Tauri commands — list_connections, create_connection, update_connection,
  delete_connection, test_connection, connect, disconnect
- Passwords stored in macOS Keychain via keyring crate (key: "esploro" / "connection:{id}")
- Pool builder using deadpool-postgres + tokio-postgres (NoTls; unix socket via host path)
- Fixed tauri.conf.json: vibrancy → windowEffects with camelCase enum values
- Frontend: TypeScript types (ConnectionProfile, ConnectionInput, SslMode)
- Frontend: API layer wrapping invoke calls + postgres:// URL parser
- Frontend: ConnectionForm — create/edit Dialog with all fields, Test Connection button,
  URL quick-fill, inline validation, color swatch picker
- Frontend: ConnectionList — sidebar items with connect/disconnect/edit/delete actions,
  green dot for active connections
- Frontend: Zustand store extended with profiles, activeSessions, pendingNewConnection
- CommandPalette: "New Connection" + "Connect to {name}" commands
- SidebarSection: added optional action prop for "+" button
- Sidebar: loads profiles on mount, wires ConnectionList + ConnectionForm
